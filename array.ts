let myArr: string[] = ["Vaibhav", "Reyna", "Omen", "Fade"];
// console.log(myArr);

// Traversing the array with for in
let x: string;
for (x in myArr) {
	// console.log(myArr[x]);
}

let myArr2: number[] = [1, 32,53,62,27,19];
console.log(myArr2);

let doubleArr: number[] = myArr2.map((x)=> x*2);
console.log(doubleArr);
